
import React from 'react';
import ReactDOM from 'react-dom';
import { BrowserRouter as Router, Route, Switch } from 'react-router-dom';
import NewPageWithPreloader from './components/NewPageWithPreloader';
ReactDOM.render(
  <Router>
    <Switch>
      <Route path="/new-page-with-preloader" component={NewPageWithPreloader} />
      {/* Add other routes here */}
    </Switch>
  </Router>,
  document.getElementById('root')
);